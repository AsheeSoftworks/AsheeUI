import type { SpacingConfig } from "./spacing";

export const defaultSpacingConfig: SpacingConfig = {
  default: "md",
  values: {
    none: "0",
    xs: "0.5rem",
    sm: "0.75rem",
    md: "1rem",
    lg: "1.5rem",
    xl: "2rem",
  },
};
