import { SubstitutionIsland } from "./substitution-island";

/**
 * The substitution page, a server component that renders the examples.
 *
 * @returns The page.
 */
export default function SubstitutionPage() {
  return <SubstitutionIsland />;
}
