import type { ParallaxConfig } from "./parallax-config";

export const defaultParallaxConfig: ParallaxConfig = {
  overlay: "dark",
  bgSpeed: 0.2,
  contentSpeed: -0.15,
  disableAnimation: false,
};
